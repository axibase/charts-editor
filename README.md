# charts-editor

## Quick start

Install dependencies:
`npm i`

Build project:
`npm run build`

Run local server:
`npm run serve`

Update language json settings from syntax core:
`npm run update-resources`
